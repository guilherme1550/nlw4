<style>
    .container {
        width: 800px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        align-content: center;
    }

    .title {
        margin-bottom: 8px;
    }

    .level {
        display: flex;
        margin: 10px;
        justify-content: space-between;
        width: 400px;
    }

    .answers {
        width: 400px;
        display: flex;
        justify-content: space-between;
        margin-bottom: 24px; 
    }
    
    
    .value {
        padding: 10px;
        background-color: #8257e6;
        color: #fff;
        border-radius: 6px;
        width: 15px;
        text-align: center;
        text-decoration: none;
    }
</style>


<div class= "container">
    
    <label>Olá <strong>{{name}}</strong>! Tudo bem?</label> 

    <h3>{{tittle}}</h3>

    <strong>{{description}}</strong>

    <div class="level">
        <span>Pouco Provável</span>
        <span>Muito Provável</span>
    </div>

    <div class="answers">
        <a class="value" href="{{link}}/1?u={{surveyUser_id}}">1</a>
        <a class="value" href="{{link}}/2?u={{surveyUser_id}}">2</a>
        <a class="value" href="{{link}}/3?u={{surveyUser_id}}">3</a>
        <a class="value" href="{{link}}/4?u={{surveyUser_id}}">4</a>
        <a class="value" href="{{link}}/5?u={{surveyUser_id}}">5</a>
        <a class="value" href="{{link}}/6?u={{surveyUser_id}}">6</a>
        <a class="value" href="{{link}}/7?u={{surveyUser_id}}">7</a>
        <a class="value" href="{{link}}/8?u={{surveyUser_id}}">8</a>
        <a class="value" href="{{link}}/9?u={{surveyUser_id}}">9</a>
        <a class="value" href="{{link}}/10?u={{surveyUser_id}}">10</a>
    </div>
    
    <strong>Sua opinião é muito importante para nós</strong>
    <h3>Equipe | <strong>NLW</strong></h3>

</div>